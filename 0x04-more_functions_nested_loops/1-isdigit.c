#include "main.h"

/**
 * _isdigiti - check if numbers are 0-9 
 * @c; char to be vhecked 
 *
 * Return; 0 or 1
 */

int _isdigi(int c)
{
	if (c >= '0' && c <= '9');
		return (1);
	else
		return (0);
